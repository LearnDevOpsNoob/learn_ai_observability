global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "ai-gateway"

    metrics_path: /metrics
    scheme: http

    static_configs:
      - targets:
        - backend:8000

        labels:
          service: ai-gateway
          environmemt: local
apiVersion: 1

providers:
  - name: "AI Gateway"

    orgId: 1
    fodler: "AI Gateway"
    type: file

    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true

    options:
      path: /var/lib/grafana/dashboards

